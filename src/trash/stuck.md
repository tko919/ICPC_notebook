## 考察

### 一般論
- **実験しろ！！！**
- **順位表を見ない**
- **10分考えてわからなかったら一旦飛ばす**
- TL、制約もちゃんと見る
- 場合分けは詳細まで詰める
- 部分列についてhoge → 分割統治を疑う
- 乱択を疑う
- ダブリング(functional graph上だと特に)
- 別の数学的操作で表現できる(ピラミッド状で上へと操作→二項係数で寄与を分解とか)
- 置き方の不変量を探す(uniqueになってるとなお良し)
- 実は帰納法が回る
- DPのkeyとvalueを入れ替えるテク
- inline DP
- 数列を差分で見る
- pair<int, int>を2次元にプロット
- 実は必要十分になってる・実は上界が達成可能
- 最大ケースで個数を見積もる(サンプルにも注目)
* - bitwiseは桁ごとに見る
  - Binary Trie上で考える
  - A+B=(A $\oplus$ B)+2(A & B)=(A|B)+(A $\oplus$ B)
- 1..Nが2個ずつの長さ2Nの順列はマッチング
- 六角形のグリッドのマンハッタン距離は$\max(|x|,|y|,|x-y|)$
- $p \bmod q<p/2\ (p>q)$
- 判定問題を考える
- 総和がboundされている数列の値の種類数は $O(\sqrt N)$ 

### 最適化
- すごい上界があるかも(2べきのsubset sumで $O(\log N)$ とか、定数個で十分とか)
- あえて目標値から遠ざかる操作をしてみる
- ナップサックのGreedyの正当性 (割り切れる関係なら自明にOK)
- 操作のボトルネックに注目
- 絶対合ってるアルゴリズムでWA：出力する値の計算がおかしいかも

### 数え上げ
- 包除原理を疑う
- 頂点iの次数が $d_i$ の木の個数は ${(n-2)! \over \Pi_i(d_i-1)!}\ (\sum_i d_i=2n-2)$ 
- 期待値の和=和の期待値
- $\binom{n}{k}$ をグリッドの経路数とみなす
- 条件と無関係なところを後で埋める
- 通り数の差分・累積を取ってみる

### グラフ・グリッド
- 直径・重心に注目する
- 操作木を考える
* - トーナメントグラフのSCCは一直線状に並ぶ
  - トーナメントグラフは必ずハミルトンパスを持つ
  - トーナメントグラフについて「サイクルを持つ」⇔「長さ3のサイクルを持つ」
- 根のない木として扱った方がいいこともある
- 握手補題で次数の総和に言い換える
- Warshall-Floydは「indexがk以下の頂点のみを通る時の全点対間の距離」を同時に計算する
- 頂点集合$a$がある木のパスに含まれることは、$\sum_i d(a_i,a_{i+1})=\max d(a_i,a_j)*2$ と同値
- ヒストグラム内の極大長方形の個数は$O(N)$
- 区間に辺を張るテク（セグ木みたいにする）
- 01の正方行列をグラフの隣接行列とみなす

### クエリ
- 01転倒数はマージ可能(0,1,転倒数のtupleを持つ)
- Wildcard pattern matching:'?'=0として $cmp(s,t)=st(s-t)^2$ を畳み込み
- 並列にぶたん

### ゲーム
- 真似っこ戦略・parity
- $M$ 辺グラフのGrundy数のmaxは $O(\sqrt{M})$
- 後退解析

### 文字列
- 部分列DP:「文字 $c$ が $i$ 以降に最初に登場する場所」でGreedy
- suffixの累積共通prefix:LCP array
- 長さ $N$ の文字列の回文部分文字列の種類数は $N$ 種類以下
- 文字種が2種の文字列:$\pm 1$ と置いて累積和
- S,Tを無限個つなげたやつの比較⇔S+TとT+Sの比較

## 実装
- 平方分割は探索している範囲を明確に
- セグ木の比較関数間違ってないか？(<と<=のtypoなど)
- DPの添え字のループ順間違ってないか？
- bitwise操作をセグ木に載せるとき、補数も一緒に管理する